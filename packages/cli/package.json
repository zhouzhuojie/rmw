{
  "name": "rmw",
  "version": "0.1.0",
  "description": "rm watermarks — detect and remove invisible Unicode from text",
  "type": "module",
  "bin": {
    "rmw": "./dist/cli.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup",
    "start": "tsx src/cli.ts",
    "prepublishOnly": "pnpm build"
  },
  "dependencies": {
    "@rmw/core": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "tsup": "^8.3.5",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  },
  "engines": {
    "node": ">=18"
  }
}
